grep in cpp

take in a file and return all lines that contain given text
color highlighting for specific part of line

if given name is a directory, return all file paths recursively that contain given text

